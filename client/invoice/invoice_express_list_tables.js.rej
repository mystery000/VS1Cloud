diff a/client/invoice/invoice_express_list_tables.js b/client/invoice/invoice_express_list_tables.js	(rejected hunks)
@@ -67,8 +67,8 @@
               templateObject.showCustomFieldDisplaySettings(reset_data);
           }).catch(function (err) {
           });
-        } else {
-          let data = JSON.parse(dataObject[0].data);
+        } else {          
+          let data = JSON.parse(dataObject[0].data);          
           // handle process here
         }
       });
